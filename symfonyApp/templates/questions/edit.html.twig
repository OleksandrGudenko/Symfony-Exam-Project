{% extends 'base.html.twig' %}

{% block main %}

    Edit question: "{{ questionData.question }}"


    {{ form_start(newQuestionForm) }}
    {{ form_label(newQuestionForm.question) }}
    {{ form_widget(newQuestionForm.question) }}
    {{ form_end(newQuestionForm) }}

{% endblock %}